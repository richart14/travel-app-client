
import React from 'react';
import { shallow } from 'enzyme';

import {TripDelete} from './tripDelete';

describe('<TripDelete />', () => {
  it('should render without crashing', () => {
    // const wrapper = shallow(<TripDelete />);
  });
});